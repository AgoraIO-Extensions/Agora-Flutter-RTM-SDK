name: agora_rtm
description: Flutter wrapper around the Agora Real Time Message SDKs for Android, iOS, macOS, and Windows.
version: 2.2.6
homepage: https://www.agora.io
repository: https://github.com/AgoraIO-Extensions/Agora-Flutter-RTM-SDK.git
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'
dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  ffi: '>=2.0.1 <3.0.0'
  async: '>=2.11.0 <3.0.0'
  meta: '>=1.9.1 <2.0.0'
  iris_method_channel: ^2.2.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: '>=1.0.0'
  build_runner: ^2.1.7
  json_serializable: ^6.1.1
  ffigen: ^8.0.2
flutter:
  plugin:
    platforms:
      android:
        package: io.agora.agorartm.agora_rtm
        pluginClass: AgoraRtmPlugin
      ios:
        pluginClass: AgoraRtmPlugin
      macos:
        pluginClass: AgoraRtmPlugin
      windows:
        pluginClass: AgoraRtmPluginCApi
